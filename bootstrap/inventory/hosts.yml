---
# Inventory. Реальные параметры хоста (IP, ansible_user, ssh-ключ) живут в
# host_vars/<имя-хоста>.yml — файл в репозиторий не уходит, рядом лежит .example.
all:
  children:
    prod:
      hosts:
        prod-vps: {}
