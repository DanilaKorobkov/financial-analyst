---
# Handlers роли common.
# В текущем виде задачи common не меняют конфигурацию сервисов,
# которым нужен бы handler (sshd через assert, не правка). Файл оставлен
# как точка расширения — если позже появится drop-in для sshd или
# конфигурация systemd-unit, handler ляжет сюда.
