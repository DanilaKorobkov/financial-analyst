# Локальный словарь cspell для скоупа web/.
# Сюда — слова, реально появляющиеся в web/** (TS/HTML/Markdown/JSON),
# и отсутствующие в общих словарях ru_ru/en_US/softwareTerms/companies.
# Формат: одно слово на строку. # — комментарий.

# --- Подключаемые библиотеки фронта ---
connectrpc           # @connectrpc/connect — runtime библиотека Connect
tabler               # @tabler/icons-react — иконпак, упомянут в правилах web/

# --- Имена инструментов разработки ---
pipx                 # Python CLI installer (упомянут в комментариях mise.toml)

# --- Доменные термины и сокращения, встречающиеся в комментариях/конфигах ---
репо                 # сокращение «репозиторий», встречается в комментариях

# --- Имена сервисов / окружения ---
Dokploy              # self-hosted PaaS для деплоя Docker-образов

# --- Доменные финансовые/биржевые термины, если встретятся в UI/тексте ---
тикер
эмитент
MOEX
SBER
GAZP
LKOH
YDEX
